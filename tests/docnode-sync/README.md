```ts
// local-first
new DocSyncClient({
  server: {
    /** */
  },
  local: {
    /** */
  },
});

// local-only
new DocSyncClient({
  local: {
    /** */
  },
});

// server-only
new DocSyncClient({
  server: {
    /** */
  },
});
```
