services:
  postgres:
    image: postgres:17
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: docukit
      POSTGRES_PASSWORD: docukit
      POSTGRES_DB: docukit
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
